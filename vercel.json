{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/support", "destination": "/support.html" }
  ],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 120,
      "memory": 1024,
      "includeFiles": "lib/**"
    }
  }
}
